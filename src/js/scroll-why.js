export default function ScrollWhy() {
    // const section = document.querySelector('.why')
    // const list = document.querySelector('.why--list')
    // console.dir(section)
    // console.dir(document)

    // list.style.height = `${window.innerHeight - 80}px`
    
    // window.addEventListener('scroll', (e) => {

    //     if (section.offsetTop + window.innerHeight <= window.scrollY) {
    //         return
    //     }
    // })
}